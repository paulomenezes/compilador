int somaImpares() {
	int i, proxImpar, resultado;
	resultado = 0;
	i = 0;
	while (i < n) {
		proxImpar = 2*i + 1; ** o i-esimo impar positivo
		resultado = resultado + proxImpar;
		i = i + 1;
	}
	prnt(resultado);
	return resultado;
}